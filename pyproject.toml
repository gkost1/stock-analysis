[project]
name = "stock-analysis"
version = "0.1.0"
description = "Stock analysis tooling using Finnhub"
authors = [
    {name = "garrettkost"}
]
readme = "README.md"
requires-python = "^3.11"
dependencies = [
    "finnhub-python (>=2.4.29,<3.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
